【应用简介】

rtp2httpd 可以将 IPTV 组播流（RTP/UDP）、RTSP 流、HTTP 流转换为普通 HTTP 流，让手机、电视盒子、电脑等任何设备无需加入组播网络即可观看 IPTV。支持运营商 FCC 快速换台，内置实时状态面板和网页播放器，可替代 udpxy / msd_lite。

【快速上手】

1. 安装并启动应用。服务默认监听 5140 端口（可在应用配置中修改）。

2. 用浏览器访问状态面板，确认服务正常运行：
   http://<本机IP>:5140/status

3. 在播放器（VLC、Kodi、电视直播软件等）中播放组播频道：
   http://<本机IP>:5140/rtp/<组播地址>:<组播端口>
   例如：http://192.168.1.1:5140/rtp/239.253.64.120:5140

   同时兼容 udpxy 风格的 /udp/ 地址，原有 udpxy 播放列表只需替换 IP 和端口即可继续使用。

4. 如果在应用配置中填写了"外部 M3U 播放列表地址"，还可以使用：
   - 网页播放器：http://<本机IP>:5140/player
   - 转换后的播放列表：http://<本机IP>:5140/playlist.m3u
     （播放列表中的组播/RTSP 地址会自动转换为本服务的 HTTP 地址）

【常用配置说明】

以下选项可在应用市场的应用配置页面中设置：

- 监听地址 / Web 与流服务端口：服务绑定的地址和端口，默认 0.0.0.0:5140。
- 上游接口：组播流量从哪块网卡接收（即 IPTV 线路所在的接口，如 eth1）。留空时使用系统路由表自动选择；如果无法收到组播流，请优先尝试设置此项。
- 外部 M3U 播放列表地址：填写运营商或自己整理的频道列表 URL，用于网页播放器和 playlist.m3u。
- 访问令牌：设置后所有请求必须携带 r2h-token 参数，建议在需要公网访问时配合使用。
- 最大客户端数 / 工作进程数：并发能力相关，默认值适合大多数家庭场景。
- 其余高级选项（缓冲区、FCC、零拷贝、视频快照等）保持默认即可，含义可参考官方文档。

【常见问题】

- 无法播放组播频道：
  请先确认爱快主机本身能收到 IPTV 组播流（IPTV 线路已接入、IGMP/组播相关设置正确）。如果系统路由无法正确选择 IPTV 接口，请在应用配置中设置"上游接口"为 IPTV 线路对应的网卡。

- 播放卡顿、丢包：
  可适当增大“UDP 接收缓冲区大小”和“缓冲池最大容量”；多设备并发观看时可增加“工作进程数”。

- 组播看一段时间后断流：
  部分交换机会超时剔除组播成员，可将“组播周期性重加入间隔”设置为 30-120 秒。

- 换台速度慢：
  运营商支持 FCC 时，可在播放地址中添加 fcc 参数实现秒换台，详见官方文档。

【更多文档与反馈】

完整文档：https://rtp2httpd.com
问题反馈：https://github.com/stackia/rtp2httpd/issues
